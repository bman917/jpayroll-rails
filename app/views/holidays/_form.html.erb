<%= form_for(@holiday) do |f| %>
  <% if @holiday.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@holiday.errors.count, "error") %> prohibited this holiday from being saved:</h2>

      <ul>
      <% @holiday.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br />
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :holiday_type %><br />
    <%= f.collection_select :holiday_type, HolidayType::NAMES, :to_s, :to_s%>
  </div>
  <div class="field">
    <%= f.label :rate %><br />
    <%= f.select :rate, [['30%','.30'],['100%','1']] %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
